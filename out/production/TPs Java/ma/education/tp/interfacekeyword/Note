1. Créer un objet à partir des interfaces fonctionnelles suivante en utilisant les expressions Lambda
    @FunctionalInterface
    interface Testable{
        boolean isCorrect(String email);
    }
Testable t =  email->true;

    @FunctionalInterface
    interface Operation{
        int addition(int a,int b);
    }
Operation o = (a,b)-> a+b;

    @FunctionalInterface
    interface Logger{
        void display(String log);
    }
Logger l = (log)->System.out.println(log);



2.Choisir les expressions Lambda correcte pour la méthode suivante:
    Integer add(Integer a,Integer b);
    1 - a,b-> a+b;
    2 - (a,b)-> a+b;
    3 - (a,b)-> return a+b;
    4 - (a,b)-> {return a+b;};
    5 - (a,b)-> {return a+b};

les expressions Lambda correcte sont 2,4